{
  "name": "xsaved-topics",
  "version": "0.1.0",
  "description": "CLI tool that generates a personalized topic taxonomy from a user's bookmarks and labels each bookmark against it, using the Claude API",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5.7.3"
  }
}
